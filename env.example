# ═══════════════════════════════════════════════════════════════════════════════
# Nightline / LSA Scraper Configuration
# ═══════════════════════════════════════════════════════════════════════════════
# Copy this file to .env and fill in your values:
#   cp env.example .env
#
# Which keys you need depends on what you're running:
#   - LSA Scraper only:    No keys needed (uses Playwright browser)
#   - AI Caller:           VAPI_API_KEY + VAPI_PHONE_ID
#   - Legacy Maps scraper: RAPIDAPI_KEY
# ═══════════════════════════════════════════════════════════════════════════════

# ── Vapi.ai (AI Caller) ──────────────────────────────────────────────────────
# Required for: overnight_caller.py, vapi_caller.py
# Sign up: https://vapi.ai → Dashboard → API Keys
VAPI_API_KEY=
# Go to: Vapi Dashboard → Phone Numbers → click your number → copy the ID
VAPI_PHONE_ID=
# Optional: auto-creates if left empty. Set this to reuse an existing assistant.
VAPI_ASSISTANT_ID=

# ── RapidAPI (Legacy Google Maps scraper) ─────────────────────────────────────
# Required for: nightline scrape city/multi (the old Maps API scraper)
# NOT needed for the new LSA scraper (nightline lsa)
# Sign up: https://rapidapi.com/letscrape-6bRBa3QguO5/api/local-business-data
RAPIDAPI_KEY=

# ── Twilio (direct calling - optional) ───────────────────────────────────────
# Only needed if using test_caller.py (raw Twilio calls without Vapi AI)
# The Vapi caller handles Twilio under the hood, so you usually don't need these
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=
TWILIO_WEBHOOK_URL=

# ── Database ──────────────────────────────────────────────────────────────────
# Defaults to SQLite (no setup needed). Change for PostgreSQL etc.
# DATABASE_URL=sqlite:///nightline.db
